# `api/domain/services/`

Use for **domain services** when behavior does not naturally belong to a single entity (e.g. cross-aggregate rules). Prefer entity methods first; keep services free of infrastructure imports.
