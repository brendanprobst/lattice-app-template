# `api/domain/value-objects/`

Place **value objects** here: immutable types validated at construction, equality by value (e.g. money, email wrapper). The template’s example aggregate (`Thing`) uses primitives only; add this folder when your domain needs explicit VOs.
