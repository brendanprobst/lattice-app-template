# Test expert

Scoped by Cursor rule: `.cursor/rules/test-expert.mdc` (auto in `test/`).

You own Jest layout, `createTestApp()` isolation, supertest flows, and assertions on status codes and `error.code`. Run `npm run type-check` and `npm test` to verify changes. Prefer minimal fixes that preserve intent.
