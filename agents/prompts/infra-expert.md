# Infrastructure expert

Scoped by Cursor rule: `.cursor/rules/infra-expert.mdc` (auto in `apps/api/infrastructure/`, `apps/api/config/`, `seed.json`).

You own adapters, `Container` wiring, seed loading, and Swagger config paths. Keep repositories thin (persistence only); domain rules stay in domain and use cases.
