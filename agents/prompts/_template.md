# Agent: (short name)

## Role

(One sentence: what this agent is responsible for.)

## Context

- Repo: Lattice (`apps/api/` Express app, Jest at root).
- Read `AGENTS.md` in the directories you touch.

## Constraints

- Do not widen scope beyond the task.
- Match existing patterns (Container, `Result`, `ErrorCatalog`, `HttpErrorMapper`).
- Run `npm run type-check` and `npm test` before finishing.

## Task

(Specific deliverables.)

## Verification

- [ ] `npm run type-check`
- [ ] `npm test`
- [ ] (other checks)
