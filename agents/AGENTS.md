# `agents/` — Agent prompt framework

Prompt stubs in `prompts/`; canonical instructions for Cursor are **`../.cursor/rules/*.mdc`**. Read **`README.md`** for path-scoped vs `@` Rules (debugger, janitor), and **Tests-first feature work** (Test expert applies to `apps/api/`, `apps/web/client/`, `apps/web/app/` plus `test/**`). `registry.yaml` lists ids and globs. **Infra expert** also scopes to **`apps/web/client/lib/analytics/**`** (web analytics adapters). Scaffold does **not** codegen adapters — see **[`docs/research/scaffold-agents-and-adapters.md`](../docs/research/scaffold-agents-and-adapters.md)**.
