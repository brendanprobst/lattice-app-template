# `agents/` — Agent prompt framework

Prompt stubs in `prompts/`; canonical instructions for Cursor are **`../.cursor/rules/*.mdc`**. Read `README.md` for path-scoped vs `@` Rules (debugger, janitor). `registry.yaml` lists ids and globs.
